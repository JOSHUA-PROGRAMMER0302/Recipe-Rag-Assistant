# Copy this file to `backend/.env` and fill in your keys. DO NOT commit real keys.
# This file previously contained real API keys. Those have been removed.
# IMPORTANT: If the keys were ever committed or pushed, rotate them immediately.

# Example placeholders (replace with your real keys in backend/.env)
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
SCALEDOWN_API_KEY=YOUR_SCALEDOWN_API_KEY_HERE
